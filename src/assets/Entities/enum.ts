export enum TransactionType {
  INCOME = 'income',
  EXPENSE = 'expense',
}

export enum NotificationStyle {
  TOAST = 'toast',
  POPUP = 'pop-up',
}

export enum NotificationType {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
}